{
  "name": "papirar-api-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "typescript": "5.9.2",
    "wrangler": "4.130.0"
  }
}
